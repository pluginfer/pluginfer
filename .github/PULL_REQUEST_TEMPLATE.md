## What this changes

<!-- One paragraph. What behavior changes, and why. -->

## Checklist

- [ ] Tests added/updated; suite passes locally (`cd v2 && python -m pytest tests/ -q`)
- [ ] No doc/README claims the code doesn't measurably back
- [ ] Bandit clean (`python -m bandit -r v2/core v2/api -ll`)
- [ ] One logical change in this PR
